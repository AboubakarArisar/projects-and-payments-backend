# ------------------------------------------------------------------
# NOTE: The main app reads env from  src/config/config.env
#       (see src/server.js -> dotenv.config({ path: "src/config/config.env" }))
# This root .env.example exists only because src/services/auth.service.js
# also calls require("dotenv").config() with the default ".env" path.
#
# Recommended: put your values in BOTH places, or keep them identical:
#   src/config/config.env   (primary)
#   .env                    (fallback used by auth.service.js)
# ------------------------------------------------------------------

# Server
PORT=5000
NODE_ENV=DEVELOPMENT

# MongoDB — base URI only, app appends "/projects-payments-db"
DB_LOCAL_URI=mongodb://pindari:pindari@ac-rx1zjs3-shard-00-00.n9scjha.mongodb.net:27017,ac-rx1zjs3-shard-00-01.n9scjha.mongodb.net:27017,ac-rx1zjs3-shard-00-02.n9scjha.mongodb.net:27017/?ssl=true&replicaSet=atlas-o2lv1i-shard-0&authSource=admin&appName=Cluster0

# JWT
JWT_SECRET=change_me_to_a_long_random_string

# Gmail SMTP (use a Gmail App Password for SMTP_PASSWORD)
SMTP_USER=aboubakar.dev@gmail.com
SMTP_PASSWORD=ppyn csot nsov kyau
SMTP_FROM_NAME=Projects And Payments
SMTP_FROM_EMAIL=aboubakar.dev@gmail.com
